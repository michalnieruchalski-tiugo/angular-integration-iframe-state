# Summary
This repository demonstrates a potential fix to the problem where TinyMCE becomes inoperable when the editor component is rendered inside material tab.
The idea is to use `preserveContent` property.
See `src/main.ts` file

# Startup
```
npm install
npm start
```

