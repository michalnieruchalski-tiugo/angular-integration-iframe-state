# Summary
This repository demonstrates a potential fix to the problem where TinyMCE becomes inoperable when the editor component is rendered in a for loop.
The fix is to force Angular to re-create each editor component.
See `src/main.ts` file

# Startup
```
npm install
npm start
```
