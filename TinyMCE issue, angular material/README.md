# Summary
This repository demonstrates a problem where TinyMCE becomes inoperable when the editor component is rendered inside material tab.
TinyMCE becomes inoperable once tab is escaped and then re-entered.
See `src/main.ts` file

# Startup
```
npm install
npm start
```

